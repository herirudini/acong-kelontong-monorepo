import { InputValidation } from './input-validation';

describe('InputValidation', () => {
  it('should create an instance', () => {
    const directive = new InputValidation();
    expect(directive).toBeTruthy();
  });
});
