import { TableColumn } from './table-column';

describe('TableColumn', () => {
  it('should create an instance', () => {
    const directive = new TableColumn();
    expect(directive).toBeTruthy();
  });
});
